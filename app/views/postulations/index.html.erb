<div class="container">
  <div class="d-flex justify-content-between align-items-center my-3">
    <h1>Postulaciones</h1>
  </div>
  <div id="postulations">
    <div class="row">
      <% @postulations.each do |postulation| %>
        <%= render postulation %>
      <% end %>
    </div>
  </div>
</div>
