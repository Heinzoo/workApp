<nav class="navbar navbar-expand-lg" style="background-color: #ac937d;">
  <div class="container-fluid">
    <img src="https://cdn-icons-png.flaticon.com/512/1651/1651682.png" height="30">
    <a class="navbar-brand text-white fw-bold ms-3" href="/">WorkApp</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Postulaciones', postulations_path, class: 'text-white fw-bold nav-link' %>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item dropdown dropstart">
            <a class="nav-link dropdown-toggle fw-bold text-white" href="/" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Hola <%= current_user.name %>
              <img src="<%= current_user.images[0].url %>" class="rounded ms-2" style="width: 35px">
            </a>
            <ul class="dropdown-menu">
              <% if current_user.admin? %>
                <li>
                  <%= link_to 'Nuevo Usuario', new_user_registration_path, class: 'dropdown-item' %>
                </li>
                <li>
                  <hr class="dropdown-divider">
                </li>
              <% end %>
              <li>
                <%= button_to 'Cerrar sesión', destroy_user_session_path, class: 'dropdown-item' , method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to 'Iniciar sesión', new_user_session_path, class: 'btn btn-secondary fw-bold' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>